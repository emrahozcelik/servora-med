# Install as /etc/servora-med/servora-med.env (root:servora-med, mode 0640).
# Never commit real secrets.

NODE_ENV=production
HOST=127.0.0.1
PORT=3000
LOG_LEVEL=info

# Single exact https origin for the public FQDN (no path).
CORS_ORIGIN=https://app.example.com

# postgresql://user@/dbname?host=/var/run/postgresql  OR password via PGPASSFILE
DATABASE_URL=postgresql://servora@127.0.0.1:5432/servora_med

SESSION_TTL_SECONDS=28800
LOGIN_RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW_MS=60000

# Required in production. loopback | 127.0.0.1 | ::1
TRUSTED_PROXY=loopback

# Optional: pin readiness to a migration version name
# HEALTH_SCHEMA_VERSION=007_sales_meeting
